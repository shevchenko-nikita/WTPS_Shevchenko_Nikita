export function loadAndDisplayStatistics(teachers, category) {
    let myPieChart = document.getElementById("myPieChart");
    if (category === "Region") {
    }
    if (category === "Age") {
    }
    if (category === "Gender") {
    }
    if (category === "Favorite") {
    }
}
